apiVersion: v2
name: lerobot-agent-console
description: LeRobot Agent Console — a GPU StatefulSet with a per-pod PVC, fronted by APIG.
type: application
# version    = the chart's own version; bump on any template change.
# appVersion = informational only. The image tag comes from values (image.tag), because it is
#              bumped far more often than the chart and CI sets it per build.
version: 0.3.0
appVersion: "1.0"
