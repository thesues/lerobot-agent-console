nameOverride: ""

image:
  repository: docker.m.daocloud.io/livekit/livekit-server   # daocloud mirror (CN)
  # Pin a version. `latest` means a restart can silently move a PUBLIC SFU to a new server
  # build — including across a breaking change — at a moment nobody chose. This is the version
  # the running deployment resolved `latest` to, so pinning it changes nothing today.
  tag: v1.13.5
  pullPolicy: IfNotPresent

# REQUIRED: the ICE candidate address clients are told to connect to. It must be the PUBLIC IP
# of this release's CLB — not a pod IP, not a node IP — or signalling succeeds and media never
# establishes, which looks like the server working and the call being broken.
#
# The chart creates that CLB, so this is a TWO-STEP install:
#   1. install with any placeholder here; the CLB is created and takes a minute or two to be
#      assigned an address
#   2. kubectl get svc <release>-clb -o jsonpath='{.status.loadBalancer.ingress[0].ip}'
#      (or read it off the CLB console)
#   3. put it here and upgrade
#
# It cannot be discovered automatically. LiveKit's use_external_ip finds the address via STUN,
# i.e. the pod's EGRESS ip — measured on this cluster that is 124.174.58.65 while the CLB's
# INGRESS ip is 115.190.7.216. Handing clients the egress address gives them somewhere nothing
# listens. An earlier version of this chart polled the Kubernetes API from an init container
# instead; it worked, but it was a shell script parsing JSON with BusyBox tools, and it shipped
# two bugs (GNU wget flags, then a pattern that assumed compact JSON) before this line was
# reached. A value you can read and paste is worth more than an automation you cannot debug.
nodeIp: ""

# API credentials. Set them — the chart refuses to install otherwise, and that is deliberate:
# this SFU sits on a public CLB, so it must never come up with a guessable or absent key.
#
# Format is LiveKit's own `keys` map, one `api_key: api_secret` pair per line. Generate a secret
# with something like `openssl rand -hex 32`; the same pair goes into every client's config.
#
#   keys: |
#     devkey: <a long random string>
#
# The chart renders this into a Secret it owns and injects it as LIVEKIT_KEYS, which overrides
# the ConfigMap's `keys` block — a key committed to a config file is a public key.
# (It used to expect a hand-made Secret, with `kubectl create secret` in this comment. That
# cannot be done from the VKE console's Helm page, which has no shell, so the chart was simply
# not installable there.)
auth:
  keys: ""

service:
  # REQUIRED, and specific to your cluster: the CLB is created from this subnet, which must be
  # in the cluster's VPC. The value below is an example from one account — it will not exist in
  # yours, and the CLB then never gets an IP (the init container waits, then fails with that
  # hint). Find it in the VPC console, or from an existing Service's annotations.
  #
  # ⚠️ Once a CLB exists, changing this provisions a NEW load balancer with a NEW public IP and
  # invalidates every client's --livekit-url. Treat it as immutable after the first install.
  subnetId: ""
  addressType: internet
  ports:
    signal: 7880    # WS signaling
    rtcTcp: 7881    # WebRTC over TCP (fallback)
    rtcUdp: 7882    # WebRTC media (primary)

resources:
  requests: { cpu: 200m, memory: 256Mi }
  limits:   { cpu: "1",  memory: 1Gi }
